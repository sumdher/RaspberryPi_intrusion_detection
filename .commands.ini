# stop it
sudo systemctl stop wids

# enable auto-start on boot
sudo systemctl enable wids

# prevent it from starting on next boot
sudo systemctl disable wids

# bring it back
sudo systemctl start wids   # one-time start